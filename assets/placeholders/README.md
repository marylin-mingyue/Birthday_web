Public repo uses placeholders only.

Put your own private assets (photos, gifts, character art) in:
- `assets/photo/`
- `assets/gift/`
- `assets/klee.png` (or change `index.html`)

Then update `main.js` (`CONTENT`) to point to your files.


